{
  "name": "bast-website",
  "version": "0.1.0",
  "private": true,
  "description": "Static public website for Bast AI.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "verify": "pnpm build && pnpm check",
    "preview": "python3 -m http.server 8080 --directory dist"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
